extends partials/templates/_template.pug

block content
  +b.SECTION.page
    +e.flex-wrapper
      +e.left   
        // Left sidebar
        include partials/components/_left-sidebar.pug 

      +e.page__center
        // Nav
        include partials/components/_nav.pug
        // Content
        +b.SECTION.content
          +e.H1.title Страница не найдена
          +e.text 
            p Ошибка 404
            p Возможные причины ошибки:
            ul
              li ошибка при наборе адреса страницы (URL);
              li переход по неработающей или неправильной ссылке;
              li отстутствие запрашиваемой страницы на сайте;
            a(href='index.html') Вернуться на главную страницу  
                          
      +e.page__right
        // Right sidebar
        include partials/components/_right-sidebar.pug 






